// constants/url.js — API única, un solo ambiente

export const BASE_URL = "https://back.carbycol.com/api/";

// Se mantiene por compatibilidad con código existente (no-op)
export const configureUrl = async () => {};
